/**
 * @format
 */

import {AppRegistry} from 'react-native';
import WalletApp from './WalletApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => WalletApp);
